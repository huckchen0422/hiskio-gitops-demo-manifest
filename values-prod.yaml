# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

nginx:
  replicaCount: 3
  image:
    repo: huckchen0422/hiskio-gitops
    tag: 9f21ea1
  pullPolicy: Always
  prefix: prod
hook: false
